# 信頼性表示・状態管理 v2

## 目的

公開可否と災害情報の現況を分離し、利用者が「現在も有効か」「誰が確認したか」「位置はどの程度正確か」を判断できるようにする。

## データフロー

```text
登録済み情報源 → raw_items → extracted_items → review/published
                                      │
                                      ├─ 信頼性メタデータ
                                      └─ report_revisions（更新・訂正履歴）
```

`extracted_items.status`は公開ワークフロー（review / published / rejected / expired）を表す。`lifecycle_status`は現況（active / ongoing / resolved / expired / needs_review）を表す。両者を混用しない。

## 追加フィールド

- `lifecycle_status`: 発生中、継続、解消、期限切れ、要再確認
- `information_class`: 公式、報道、参考
- `location_precision`: 正確な地点、地域代表点、推定位置、位置不明
- `last_verified_at`: 人が原文と状態を最後に確認した日時
- `valid_until`: 有効期限
- `last_changed_at`: 状態・信頼性項目の最終変更日時
- `report_revisions`: 公開可能な変更理由と内部監査情報を分離して保存

## 自動公開 v2

次をすべて満たす場合だけ条件付き自動公開する。

1. 自動公開が有効
2. 公式情報源かつ許可ホスト
3. 情報源・フィード・原文が同一ホスト
4. 分類済みで信頼度が基準以上
5. 発表から24時間以内で、未来日時ではない
6. 位置が地域代表点以上で、位置不明ではない
7. 救助要請、避難指示、通行可否など高影響表現を含まない

ニュース、集約サービス、古い情報、位置不明、高影響情報はレビューへ送る。自動公開には種別別の有効期限を設定し、期限後は定期処理で`expired`へ変更する。

## 既存データ移行

`migration-007-trust-metadata.sql`は列と履歴テーブルを追加し、既存レコードを削除せずに分類する。

- 公式／報道／参考を既存ソースから推定
- 粗い市区町村座標は地域代表点、機械取得座標は推定位置として扱う
- 既存のレビュー済み座標のみ正確な地点として扱う
- 過去の有効期限切れ情報を`expired`へ移す
- 全レコードに移行履歴を1件作成
- 旧ポリシーで自動公開された古い・位置不明・高影響情報は要再確認へ戻し、履歴には残す

Cloudflare D1 Time Travelのブックマークを取得してから本番適用し、件数一致を確認する。

## 受け入れ条件

- 移行前後で`extracted_items`の総件数が一致する
- 全レコードに5種類の状態、3種類の情報区分、4種類の位置精度のいずれかが入る
- 位置不明の情報は地図にピン表示されない
- 期限切れ情報は現在情報APIから除外され、履歴APIには残る
- 公開カードに発表・取得・最終確認・有効期限、発表元、原文URL、履歴件数が表示される
- 管理画面の変更は`report_revisions`と`audit_events`の両方に記録される
- 高影響、古い、位置不明、非公式の情報は自動公開されない
- 管理操作はBearer認証と既存のレート制限を通る
- 既存の自動取得、履歴、CSV、GeoJSON、気象表示が回帰しない

## 将来見直す点

データ量が増えたら、同一事象を束ねるイベントID、複数一次情報の突合、運用者別アカウント、監視通知を追加する。無料枠ではD1とCronの範囲を優先し、外部キューは導入しない。
