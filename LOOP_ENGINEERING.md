# まもるマップ 抽出・検証・公開ループ仕様書 v0.1

## 1. 目的

公式発表と報道情報を定期的に取得し、災害支援に役立つ項目を抽出する。ただし、AIや自動処理の結果を無条件に公開せず、出典・不確実性・確認状態を利用者に伝える。

## 2. ループ全体

```text
観測 → 取得 → 抽出 → 正規化 → 検証 → 重複・矛盾確認
                         ↓
                    人手レビュー
                         ↓
                  公開・更新・失効
                         ↓
                       監視
```

## 3. 各ステップ

### A. 観測

- 有効化された公式RSS、公式API、公式ページ、ニュースRSS/APIを定期確認する
- 前回取得時刻とETag／Last-Modifiedがあれば利用する
- 同じ記事・発表を重複取得しない

### B. 取得

- 原文URL、取得日時、HTTPステータス、取得元を保存する
- タイムアウト、リトライ回数、レート制限を設定する
- 取得失敗時は既存の公開情報を勝手に削除しない

### C. 抽出

次の項目を抽出する。

- タイトル
- 要約
- 情報カテゴリ
- 地域名
- 緯度・経度または位置の候補
- 発表日時
- 重要度
- 期限・有効期間
- 原文URL
- 情報源名

抽出できない値は推測せず `unknown` にする。

### D. 正規化

- 日時をISO 8601に統一する
- 地域名を自治体コード等に紐付ける
- 情報カテゴリを固定値に変換する
- 位置を住所・施設・区域のいずれかとして保持する
- 位置が個人宅を特定しうる場合は公開位置を粗くする

### E. 検証

自動検証で以下を確認する。

- 原文URLが存在する
- 情報源が登録済みである
- 発表日時が未来になっていない
- 地域が対象エリア内または対象外として明示されている
- 位置情報が地理的に妥当である
- 個人情報らしき文字列がない
- 既存情報と重複していない
- 公式情報とニュース情報のカテゴリが混同されていない

### F. 人手レビュー

次の場合は必ず人手確認を要求する。

- 被害、救助、避難、通行可否を断定する
- 公式情報とニュースが矛盾する
- 位置が曖昧である
- 個人情報やセンシティブな内容が含まれる
- 抽出信頼度が閾値未満である
- 内容が訂正・削除されている

### G. 公開

公開項目には必ず次を表示する。

- 情報カテゴリ：公式発表／報道情報
- 情報源名
- 原文タイトルまたは発表名
- 原文URL
- 発表日時
- 最終取得日時
- 自動抽出／人手確認済み
- 更新・失効状態

### H. 更新・失効

- 同一ソースの新しい発表を既存項目に紐付ける
- 訂正情報は新しい履歴として保存する
- 有効期限を過ぎた情報は非表示または「期限切れ」と表示する
- 失効しても監査用の履歴は削除しない

## 4. 停止条件

次のいずれかに該当したら、当該項目の自動公開を停止する。

- 原文を取得できない
- 出典が不明または登録されていない
- 公式情報と報道情報が矛盾する
- 位置を安全に特定できない
- 個人情報が含まれる
- 自動抽出の信頼度が低い
- 同じ情報の異常な大量取得が発生する
- APIコストまたは取得回数が設定上限に近づく

## 5. 役割分担

| 役割 | 担当 |
|---|---|
| Collector | 情報源から取得 |
| Extractor | 項目を抽出・要約 |
| Checker | スキーマ、重複、矛盾、安全性を確認 |
| Human reviewer | 公開可否を判断 |
| Publisher | 承認済み情報を公開 |
| Monitor | 失敗、遅延、古い情報、コストを監視 |

## 6. MVPの終了条件

- 情報源を3つ以上登録できる
- 公式情報と報道情報を分けて保存できる
- 原文URL・発表日時・取得日時を全項目に付与できる
- 自動抽出結果をレビュー待ちにできる
- 人手確認後だけ公開できる
- 失敗時に公開済み情報を壊さず停止できる
- 取得履歴と変更履歴を確認できる
- スマートフォンの地図と一覧で出典を確認できる
