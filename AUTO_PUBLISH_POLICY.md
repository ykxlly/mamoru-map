# 条件付き自動公開ポリシー v2

## 有効範囲

このポリシーは、2026-08-03の本番Workerに対して有効。新規取得項目に加え、管理者が明示的に一括承認を実行した既存レビュー待ち項目にも適用する。

## 自動公開する条件

次の条件をすべて満たす場合だけ `published` にする。

- `AUTO_PUBLISH_ENABLED=true`
- 情報源のカテゴリが `official`
- 許可ホストに属し、情報源URL・フィードURL・原文URLが同一ホスト
- 原文URLがHTTPSで存在
- 抽出分類が `other` ではない
- 信頼度が `AUTO_PUBLISH_MIN_CONFIDENCE` 以上（現在 `0.82`）
- 発表日時が取得時点から24時間以内
- 位置精度が「地域代表点」以上で、位置不明ではない
- 救助要請、避難指示、通行止め、孤立など高影響表現を含まない

## 表示・期限

- `auto_published=1` として保存
- reviewerは `system-auto`
- 監査ログに `auto_publish` を記録
- 公開画面には「自動公開・未確認」と表示
- 種別別の有効期限を設定し、期限後は`expired`として現在情報APIから除外

## 自動公開しないもの

- 条件を満たさない既存のレビュー待ち項目
- ニュースカテゴリ
- 分類できない項目
- 信頼度が基準未満の項目
- 許可ホスト外または原文URL不備の項目
- 発表日時不明、24時間より古い項目
- 位置不明の項目
- 人命・避難・通行判断に直接影響する項目

## 停止・見直し条件

誤分類、古い情報の表示、原文URLの変更、ソース仕様変更が確認された場合は、Cloudflare Workerの `AUTO_PUBLISH_ENABLED` を `false` に変更して再デプロイする。自動公開された情報も、公開マップ上で原文URLと「自動公開・未確認」を必ず表示する。
